# Build artifacts
/_build/
/deps/
/cover/
/doc/
*.ez

# Compiled assets (regenerated by `mix assets.build`)
/priv/static/assets/

# JS deps (regenerated by `npm install`); package-lock.json IS committed
/assets/node_modules/

# Editor / tooling
/.elixir_ls/
.DS_Store

# Local screenshots / scratch from verification
/.verify-*.png
/.boot-*.png

# ExUnit @tag :tmp_dir scratch
/tmp/

# User's local graph sources (copy from config/sources.example.json)
/config/sources.local.json
